@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}
@model IEnumerable<AsansorTakipV3.Models.Sinif>
<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Envanter Ürünleri</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-body">
                    <div class="form-group">
                        <a href="@Url.Action("Detay","Envanter")"><input type="submit" class="btn btn-primary" value="Yeni Envanter Ekle"></a>
                    </div>
                    <table width="100" class="table table-striped table-bordered table-hover" id="dataTables-example">
                        <thead>
                            <tr>
                                <th>Marka</th>
                                <th>Sınıf</th>
                                <th style="width:19px;"></th>
                                <th style="width:19px;"></th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var item in Model.OrderBy(o => o.Marka_Id))
                            {
                                <tr class="odd gradeX">
                                    <td>@item.Marka.Adi</td>
                                    <td>@item.Adi</td>
                                    <td><a href="@Url.Action("Detay", "Envanter", new { Id = item.Id })"><i class="icon icon-edit"></i></a></td>
                                    <td><a href="@Url.Action("DetaySil", "Envanter", new { Id = item.Id })"><i class="icon icon-delete"></i></a></td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
